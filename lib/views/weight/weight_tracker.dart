import 'package:flutter/material.dart';

class WeightTrackerPage extends StatelessWidget {
  const WeightTrackerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Weight Tracker PAge"),
    );
  }
}
